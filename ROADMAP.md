# Truth History SDK — 차세대 로드맵 (Phased Plan)

> 기준일: 2026-08-15 · 최종 커밋: `6835ea0` (한국사 사료 KB + 스트리밍 청크 분석 main 반영 완료)
> 본 문서는 오픈소스 개발자대회(한국오픈소스협회) 출품 이후의 개발 계획입니다. 일정은 대회 공고(접수 마감·본심 발표) 확정 시 조정합니다.

---

## 0. 현재 상태 요약 (완료)

| 영역 | 상태 |
|:---|:---|
| SDK 4대 모듈 (텍스트/이미지/영상/오디오) | ✅ 통합 완료 (`AnalysisResult` 단일 규격) |
| 한국사 사료 KB (연표 41건·인물 15명, 오프라인) | ✅ `truthhistory/text/knowledge.py` |
| 스트리밍 청크 분석 (`th stream`, `/api/v1/scan/stream`) | ✅ `truthhistory/video/streaming.py` |
| 크롬 확장 / MCP 서버 / Vercel 라이브 배포 | ✅ (PR #1, #2) |
| 테스트 | ✅ 55개 통과 (단, CI 자동화 없음) |

---

## Phase 1 — 출품 완성도 강화 (8월 말 ~ 9월 초) `최우선`

대회 심사는 "완성도 + 설명력 + 실증"에 가장 큰 비중을 둔다. 신규 기능보다 **증거 자료와 품질**을 먼저 확보한다.

| # | 작업 | 완료 기준 (Acceptance) | 우선순위 |
|:--|:---|:---|:--|
| 1.1 | **벤치마크 데이터셋 구축** — LLM 할루시네이션 한국사 샘플 100건+ (연도 오류·시대착오·허구 인물), 정상 대조군 100건+ | `benchmarks/` 디렉터리 + 측정 스크립트로 KB·텍스트 분석기 정확도/오탐률 수치화 | ★★★ |
| 1.2 | **정확도 리포트** — 1.1 데이터로 모듈별 성능표 작성 (KB 연표 검증 적중률, ELA/딥페이크 탐지율) | `REPORT.md` 또는 `docs/benchmark.md`에 재현 가능한 측정 결과 게재 | ★★★ |
| 1.3 | **시연 시나리오·데모 영상** — ① 확장 프로그램 실시간 배지 ② `th stream` 라이브 분석 ③ MCP Claude 연동 ④ 대시보드 업로드 | 3분 내 데모 영상 1편 + 단계별 시나리오 문서 | ★★★ |
| 1.4 | **발표 자료** — 문제 정의 → 아키텍처(PROJECT_PIPELINE.md 다이어그램 재활용) → 실증 → 기대효과 | 발표 슬라이드 초안 (대회 양식 확인 후 적용) | ★★☆ |
| 1.5 | **안정화 QA** — 외부 API 장애 시 타임아웃/폴백 점검, 대시보드 E2E, 확장 프로그램 타 사이트 호환성 | 크래시 0건 확인 기록 (이슈로 정리) | ★★☆ |

## Phase 2 — 기능 확장 (9월 ~ 10월 초)

로드맵 공약 항목. 각각 독립 PR로 분리해 리스크를 격리한다.

| # | 작업 | 완료 기준 | 우선순위 |
|:--|:---|:---|:--|
| 2.1 | **국사편찬위원회 공개 데이터 직접 연동** — 공공데이터포털(data.go.kr) '한국역사자료 메타데이터' 오픈 API 활용 신청 → `search_datagokr()` 증거 소스 추가 | `DATA_GO_KR_SERVICE_KEY` (.env) 설정 시 KB 외 실제 사료 메타데이터가 증거로 병합 + 키 없으면 기존 동작 유지 | ★★★ |
| 2.2 | **KB 커버리지 확대** — 연표 41건 → 200건+ (조선 실학·개화기·항일운동 세부), 인물 15 → 60인+ | KB 미등록률 감소를 1.1 벤치마크로 검증 (회귀 방지) | ★★☆ |
| 2.3 | **VSCode Extension** — 편집기 내 한국사 텍스트 실시간 고증 검증 (decorations + 사이드 패널) | 마켓플레이스 게시 또는 `.vsix` 배포 + 사용 가이드 | ★★☆ |
| 2.4 | **오디오 스트리밍 분석** — `th stream --audio` (라이브 마이크/스트림 청크별 복제 음성 탐지) | `StreamingAudioAnalyzer` + 청크 타임라인 (영상 스트리밍과 동일 규격) | ★★☆ |
| 2.5 | **대시보드 스트리밍 탭** — React에 [파일 업로드/URL] 탭과 별도 [실시간 스트리밍] 탭, 청크 타임라인 차트 | `/api/v1/scan/stream` 연동 UI + 청크별 게이지 시각화 | ★☆☆ |
| 2.6 | **MCP 스트리밍 도구** — `scan_video_stream` 도구 추가 | Claude MCP 클라이언트에서 청크 타임라인 반환 확인 | ★☆☆ |
| 2.7 | **Hugging Face 연동** — 한국어 GPT 감지기(예: korean-ai-detector) 로컬 다운로드 경로 지원 | `backend: "hf"` 설정 시 Perplexity 폴백 대신 실모델 사용 | ★☆☆ |

## Phase 3 — 생태계·운영 기반 (10월 ~ )

심사 이후 지속 가능성 확보. 대회 이후에도 순차 진행.

| # | 작업 | 완료 기준 |
|:--|:---|:---|
| 3.1 | **CI 자동화** — GitHub Actions: push/PR 시 pytest 매트릭스(OS 3종) | 녹색 배지 + 실패 시 머지 차단 |
| 3.2 | **PyPI 정식 배포** — `pip install truth-history-sdk` | 패키지 게시 + README 설치 절차 갱신 |
| 3.3 | **크롬 웹스토어 게시** — 아이콘 5종·스크린샷·개인정보 처리방침 | 심사 통과 및 공개 URL |
| 3.4 | **영문 README** — `README.en.md` + 상단 언어 전환 링크 | 번역 완료 및 링크 정합 |
| 3.5 | **기여 가이드라인** — CONTRIBUTING.md, 이슈/PR 템플릿, 코드 오너 지정 | 외부 기여자 워크플로 문서화 |
| 3.6 | **KB 데이터 기여 프로세스** — 연표·인물 항목 PR로 추가 가능한 스키마/검증 스크립트 | KB JSON 스키마 문서 + `th kb validate` CLI |

---

## 우선순위 판단 근거

- **1.1 벤치마크가 최선순위인 이유**: "정밀 탐지"를 표방한 출품 주제 특성상 정량 근거 없이는 설득력 부족. 모든 후속 개선(2.2 KB 확대 포함)의 성과 측정 기준선이 됨.
- **2.1 공공데이터포털이 Phase 2 최선순위인 이유**: README 로드맵 공약("국사편찬위원회 직접 연동")의 완결이며, 활용 신청 승인 지연 가능성이 있어 **9월 초 즉시 신청** 후 대기 중 2.2를 병행.
- **2.3 VSCode Extension은 후순위**: 확장 프로그램(크롬)·MCP로 "실시간 가드" 스토리는 이미 충족. 신규 채널은 차별화 요소이나 심사 임팩트 대비 공수가 큼.

## 리스크 및 대응

| 리스크 | 대응 |
|:---|:---|
| 공공데이터포털 API 승인 지연 | 키 불필요한 내장 KB·위키백과가 이미 동작 — 연동은 부스트 요소로만 설계 |
| Vercel 서버리스 225MB 제한 (멀티미디어 불가) | 멀티미디어는 로컬(`th scan`/`th stream`) 경로 안내 유지, 2.5 대시보드 탭은 로컬 개발 서버 기준 |
| 외부 검색 소스 정책 변경(DuckDuckGo HTML 등) | `gather_evidence` 병렬 구조 — 단일 소스 장애 시 KB·위키백과가 판정 유지 |
