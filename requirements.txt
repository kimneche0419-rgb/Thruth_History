# Truth History SDK - Vercel 서버리스 백엔드 의존성 (텍스트 중심, 최소)
# 서버리스 함수 225MB 한계 → opencv / numpy / pillow / torch / librosa 제외.
# ✅ /api/v1/scan/text, /api/v1/scan/url 정상 동작 (확장 프로그램·대시보드 텍스트 검증)
# ⚠️ 이미지/영상/오디오는 지연 임포트 실패 시 중립값 폴백 (전체 멀티미디어는 Render/Railway 권장)
fastapi>=0.100.0
pydantic>=2.0.0
requests>=2.31.0
python-multipart>=0.0.6
